---
layout: base
---

%div#left-column.span6
  %article
    %h2.pagetitle #{page.title}
    .body
      ~ content
  .comments
    = page.intense_debate_comments
%div#right-column.span2
  .post
    .byline
      .author <strong>#{page.author}</strong>
      .date #{page.timestamp.strftime( '%d %B %Y %H:%M %Z' )}
      .tags
        - unless page.tags.empty?
          - page.tags.each do |tag|
            %a{:href=>tag.primary_page.url}
              = tag
  #twitter{:style => 'margin-top: 10px;'}
    :plain
      <a href="http://twitter.com/share" class="twitter-share-button" data-count="horizontal" data-via="marekgoldmann">Tweet</a><script type="text/javascript" src="http://platform.twitter.com/widgets.js"></script>
  #googleplus
    :plain
      <div class="g-plusone" data-size="medium"></div>

      <script type="text/javascript">
        (function() {
          var po = document.createElement('script'); po.type = 'text/javascript'; po.async = true;
          po.src = 'https://apis.google.com/js/plusone.js';
          var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(po, s);
        })();
      </script>

